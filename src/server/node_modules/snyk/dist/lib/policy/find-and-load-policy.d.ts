import { PackageExpanded } from 'snyk-resolve-deps';
import { SupportedPackageManagers } from '../package-managers';
import { PolicyOptions } from '../types';
export declare function findAndLoadPolicy(root: string, scanType: SupportedPackageManagers | 'docker', options: PolicyOptions, pkg?: PackageExpanded, scannedProjectFolder?: string): Promise<string | undefined>;
