import { OpaWasmInstance, IacFileData, IacFileScanResult } from './types';
export declare function buildPolicyEngine(): Promise<PolicyEngine>;
declare class PolicyEngine {
    private opaWasmInstance;
    constructor(opaWasmInstance: OpaWasmInstance);
    private evaluate;
    scanFiles(filesToScan: IacFileData[]): Promise<IacFileScanResult[]>;
}
export {};
